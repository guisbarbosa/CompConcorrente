#include <stdio.h>
#include <omp.h>

int main() {
    #pragma omp parallel
    {
        int tid = omp_get_thread_num();
        int total_threads = omp_get_num_threads();
        printf("Alo da thread\n Thread %d de %d threads\n", tid, total_threads);
    }
    return 0;
}
